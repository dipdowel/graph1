[package]
name = "graph1"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
minifb = "0.25.0"
rand = "0.9.0-alpha.1"
once_cell = "1.19.0"
# hound = "3.5.1"  # WAV audio
#rand = "0.8.5"
### AUDIO LIBRARIES ###
#cpal = "0.15.3"
#hound = "3.5.1"
# They currently fail to get installed. See https://github.com/Spotifyd/spotifyd/issues/659



[profile.release]
debug = false
#opt-level = 'z' # or 's' for size optimization with a balance between size and speed
opt-level = 's' # or 's' for size optimization with a balance between size and speed
lto=true
