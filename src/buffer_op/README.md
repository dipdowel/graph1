# Module `buffer`

1. This module provides low-level buffer manipulation functions for the Graph1 library. 
Instead of accepting GraphContext, these functions work with raw buffers.

2. This module also contains some experimental GPU support, see the `gpu` folder.